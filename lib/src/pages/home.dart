import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  //toda clase inmutable (StatelessWidget)sus valores y campos tienen que ser final

  @override
  Widget build(BuildContext context) {}
}
